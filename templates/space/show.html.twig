{% extends 'base.html.twig' %}

{% block title %}Space{% endblock %}

{% block body %}
    <h1>Space</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ space.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ space.name }}</td>
            </tr>
            <tr>
                <th>Photos</th>
                <td>{{ space.photos }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ space.surface }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ space.category }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ space.capacity }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ space.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('space_index') }}">back to list</a>

    <a href="{{ path('space_edit', {'id': space.id}) }}">edit</a>

    {{ include('space/_delete_form.html.twig') }}
{% endblock %}
