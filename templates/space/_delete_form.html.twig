<form method="post" action="{{ path('space_delete', {'id': space.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ space.id) }}">
    <button class="btn">Delete</button>
</form>
