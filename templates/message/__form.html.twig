	{{ form_start(form) }}
    {{ form_row(form.title, {'label': 'Sujet du message'}) }}
    {{ form_row(form.message, {'label': 'Contenu'}) }}
    {{ form_row(form.envoyer, {'attr': {'class': 'user-index-submit'}}) }}
    {{ form_end(form) }}
